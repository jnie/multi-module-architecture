# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "main", "feature/**" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6
    - name: Set up JDK 21
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B verify --file pom.xml

    # JaCoCo code coverage
    - name: Generate JaCoCo coverage report
      if: always()
      run: mvn jacoco:report -f pom.xml

    - name: Upload JaCoCo coverage report
      if: always()
      uses: actions/upload-artifact@v7
      with:
        name: jacoco-report
        path: '**/target/site/jacoco/**'

    # SpotBugs static analysis
    - name: Run SpotBugs
      if: always()
      run: mvn spotbugs:check -f pom.xml

    - name: Upload SpotBugs report
      if: always()
      uses: actions/upload-artifact@v7
      with:
        name: spotbugs-report
        path: '**/target/spotbugsXml.xml'

    # Upload test reports
    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v7
      with:
        name: test-results
        path: '**/target/surefire-reports/*.xml'

    # Optional: SonarCloud analysis (requires SONAR_TOKEN secret)
    # Uncomment and add SONAR_TOKEN to enable
#    - name: Analyze with SonarCloud
#      if: env.SONAR_TOKEN != ''
#      run: mvn sonar:sonar -Dsonar.projectKey=jnie_multi-module-architecture -f pom.xml
#      env:
#        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}