spring:
  application:
    name: multi-module-architecture
  r2dbc:
    url: r2dbc:h2:mem://./mma-cache;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always
# application properties
mma:
  outbound:
    advice-slip-api:
      url: "https://api.adviceslip.com"
  cache:
    enabled: true